assume Btwn_f root x null;
assume x ~= null;

prev := null;
curr := root;

assume curr ~= x;
prev := curr;
curr := f curr;

assume curr ~= x;
prev := curr;
curr := f curr;

cut: 

assume curr = x;

f prev := f x;

assume root ~= x;

assume ~ Btwn_f root null null;
