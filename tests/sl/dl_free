method dl_free(lst)
    requires dlseg(next, prev, lst, a, null, b);
    ensures emp;
{
    prv := a;
    while (lst != null) 
        invariant dlseg(next, prev, lst, prv, null, b);
    {
        prv := lst;
        lst := lst.next; 
        free prv;
    }
}
