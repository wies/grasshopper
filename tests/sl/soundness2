method soudness_check2(x)
    requires lseg(next, x, null);
    ensures lseg(next, x, null);
{
    curr := x;
    while (curr != null) 
       invariant lseg(next, x, curr) * lseg(next, curr, null);
    {
       assert !true;
       curr := curr.next;
    }
}
