method free_(lst)
    requires slseg(next, data, lst, null);
    ensures emp;
{
    while(lst != null) 
        invariant slseg(next, data, lst, null);
    {
        curr := lst;
        lst := lst.next; 
        free curr;
    }
}
