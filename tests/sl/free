method free_(lst)
    requires lseg(next, lst, null);
    ensures emp;
{
    while(lst != null) 
        invariant lseg(next, lst, null);
    {
        curr := lst;
        lst := lst.next; 
        free curr;
    }
}
