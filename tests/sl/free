method free_(lst)
    requires lseg(lst, null);
    ensures emp;
{
    while(lst != null) 
        invariant lseg(lst, null);
    {
        curr := lst;
        lst := lst.next; 
        free curr;
    }
}
