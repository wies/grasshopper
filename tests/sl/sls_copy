method copy(lst)
    requires slseg(lst, null);
    ensures slseg(lst, null) * slseg(returned, null);
{
    if (lst == null) {
        return null;
    } else {
        curr := lst;
        new head;
        cp := head;
        cp.data := curr.data;
        cp.next := null;
        while(curr.next != null) 
            invariant lslseg(lst, curr, curr);
            invariant slseg(curr, null);
            invariant lslseg(head, cp, curr);
            invariant cp |-> null;
            invariant cp.data == curr.data;
            invariant curr != null;
        {
            old_cp := cp;
            new cp;
            old_cp.next := cp;
            curr := curr.next;
            cp.data := curr.data;
            cp.next := null;
        }
        return head;
    }
}
