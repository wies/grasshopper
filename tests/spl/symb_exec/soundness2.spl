// Expect these tests to fail

procedure foo(a: Int) returns (b: Int)
  requires a == 2
  ensures b == 4

procedure pure_call1(c: Int, d:Int)
  requires c == 2 && d == c
  ensures c == 4
{
  c := foo(c);
  assert false;
}
