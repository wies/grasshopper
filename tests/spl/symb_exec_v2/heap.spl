options "-symbexec-v2"

struct Node {
  var next: Node;
}

procedure lookup_heap(x: Node, y: Node)
  requires acc(x) &*& acc(y) &*& x.next == null
  ensures acc(x) &*& acc(y) &*& x.next == y
{
 y := x.next;
}
