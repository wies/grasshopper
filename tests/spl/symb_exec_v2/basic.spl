options "-symbexec-v2"

/*
procedure assign(x: Int, y: Int)
  requires x == 0 && y == x
  ensures x == 0 && y == x
{
  pure assert x == 0;
}
*/

procedure assign2(x: Int, y: Int) 
  requires x == 10 && y == 5 
  ensures x == 10 && y == 5
{
  x := 10;
  y := 5;
}
