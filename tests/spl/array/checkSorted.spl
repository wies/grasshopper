include "int_array.spl"

procedure chechSorted(a: Array<Int>)
  requires sorted_array(a)
  ensures  sorted_array(a)
{
  var i := 0;
  
  while (i < a.length - 1)
    invariant i >= 0
    invariant sorted_array(a)
  {
    pure assert(a[i] <= a[i+1]);
    i := i + 1;
  }
}
