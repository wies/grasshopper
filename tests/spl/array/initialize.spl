include "int_array.spl";

procedure initialize()
  returns (a: Array<Int>)
  requires emp
  ensures array(a)
{
  a := new Array<Int>(10);

  var i := 0;
  
  while (i < a.length)
    invariant i >= 0
    invariant array(a)
  {
    a[i] := i;
    i := i + 1;
  }
}
