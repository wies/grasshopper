procedure initialize ()
  returns (a: Array<Int>)
  requires emp
  ensures acc(a) &*& arrayseg(a, 0, a.length)
{
  a := new Array<Int>(10);

  var i := 0;
  
  while (i < a.length)
    invariant i >= 0;
    invariant acc(a) &*& arrayseg(a, 0, a.length)
  {
    a[i] := i;
    i := i + 1;
  }
}
