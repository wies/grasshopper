struct Node { }

struct HashSet {
  var buckets: Array<Node>; /* the set elements */
}

procedure hashset_create(_size: Int) returns (res: HashSet)
{
  var buckets := new Array<Node>(_size);    
}


procedure hashset_free(hashset: HashSet)
{
  free(hashset.buckets);
}
